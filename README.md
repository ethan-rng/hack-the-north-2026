# Crowd Control

[Product requirements](docs/Crowd-Control-PRD.md)

The Jev service lives in [`services/jev-worker`](services/jev-worker). See the [team API guide](docs/jev-api.md) for the deployed endpoint, credentials handoff, request examples, verification, and deployment commands. The Worker is connected to the `crowd-control` AI Gateway. Live inference currently needs AI Gateway credits; details are in the guide.
