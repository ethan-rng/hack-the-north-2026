{
  "name": "crowd-control-jev",
  "version": "1.0.0",
  "description": "Team endpoint for Jev through Cloudflare Workers AI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "node --test scripts/api.test.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  }
}
